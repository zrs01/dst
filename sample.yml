---
# column definition:
# column name, data type, is primary key, is unique value, is nullable, foreign hint, description

fixColumns: &fc
  - [createUser, char(10), n, n, n, userProfile, user of create creation]
  - [createTime, datetime, n, n, n, userProfile, time of record creation]

schemas:
  - name: schema-name
    tables:
      - name: table1
        description: "table description"
        columns:
          - [name, type, n, n, n, foreign, desc]
          - *fc

      - name: table2

